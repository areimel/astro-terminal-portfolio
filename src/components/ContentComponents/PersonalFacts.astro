---
import DecoderText from "@components/AnimationComponents/DecoderText.astro";

const facts = [
    {
        prefix: "//01_",
        title: "Approach",
        content: `I believe great software starts with understanding the problem, not jumping to solutions.
        <br/><br/>
        My development process combines technical expertise with user-centered thinking to create applications that are both powerful and intuitive.
        <br/><br/>
        Every line of code is written with maintainability, performance, and scalability in mind.
        `
    },
    {
        prefix: "//02_",
        title: "Specialization",
        content: `
        <ul class="mt-4 space-y-4 pl-6">
          <li><strong>Performance</strong>: Optimizing applications for speed, efficiency, and exceptional user experience across all devices.</li>
          <li><strong>Scalability</strong>: Building systems that grow with your business needs while maintaining code quality and reliability.</li>
          <li><strong>User Experience</strong>: Creating interfaces that are intuitive, accessible, and delightful to use.</li>
        </ul>`
    },
    {
        prefix: "//03_",
        title: "Process",
        content: `
        <ul class="mt-4 space-y-4 pl-6">
          <li><strong>Discovery & Planning</strong>: Understanding your goals, users, and technical requirements before writing any code.</li>
          <li><strong>Iterative Development</strong>: Building in phases with regular feedback to ensure we're always on the right track.</li>
          <li><strong>Testing & Optimization</strong>: Rigorous testing and performance optimization to deliver reliable, fast applications.</li>
        </ul>`
    },
    {
        prefix: "//04_",
        title: "Results",
        content: `
        Successful projects aren't just about clean code—they're about delivering measurable business value and exceptional user experiences.
        <br/><br/>
        I focus on creating applications that not only meet technical requirements but also drive engagement, conversions, and long-term success for your business.
        `
    }
];
---

<section class="w-full p-4 md:p-8 mb-12 bg-terminal-bg-primary">
  <div class="w-full h-full max-w-screen-2xl mx-auto">
    
    <div class="text-center mb-12">
      <h2 class="text-5xl font-semibold text-terminal-500 font-uav-mono mt-2">
        About Me
      </h2>
    </div>

    <div class="facts-grid">
      {facts.map(({ prefix, title, content }) => (
        <>
          <div class="facts-cell facts-header">
            <p class="facts-prefix">{prefix}</p>
            <h3 class="facts-title">{title}</h3>
          </div>
          <div class="facts-cell facts-content">
            <div class="facts-text" set:html={content} />
          </div>
        </>
      ))}
    </div>
  </div>
</section>

<style>
  .facts-grid {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    grid-template-rows: repeat(1, 1fr);
    gap: 15px;
    width: 100%;
    min-height: 600px;
  }

  .facts-cell {
    @apply bg-terminal-bg-secondary p-6 shadow-md flex flex-col justify-start border border-terminal-300;
  }

  .facts-header {
    grid-column: span 2 / span 2;
  }

  .facts-content {
    grid-column: span 4 / span 4;
  }

  .facts-prefix {
    @apply text-5xl font-semibold text-terminal-500 font-uav-mono mb-4;
  }

  .facts-title {
    @apply text-5xl font-semibold text-terminal-500 font-uav-mono;
  }

  .facts-text {
    @apply text-terminal-400 font-kode text-xl;
    
    ::marker{
      content: "> ";
      @apply text-terminal-700;
    }
  }


  ul{
    @apply text-lg text-terminal-400 font-kode pl-4;
    
  }

  /* Responsive adjustments for smaller screens */
  @media (max-width: 768px) {
    .facts-grid {
      grid-template-columns: 1fr;
      grid-template-rows: auto;
      gap: 15px;
    }

    .facts-header,
    .facts-content {
      grid-column: 1;
    }
  }
</style> 